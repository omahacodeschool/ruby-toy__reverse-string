def reverse_characters(str)
    #create an array dividing up the string into individual characters
    array = [str.split("")]
    #create a holder for to-be-reversed characters
    reverse_array = []
    #going through each character in the array, store it in the last position of the new holder array
    loop.each do
        reverse_array[array[-1,1]]
    end
  return reverse_array
end


def reverse_words(str)
    
end
